{{#each productos_disponibles}}
  <div class="mb-4 justify-content-center">
    <div class="card">
      <img src="img/{{this}}.png" class="card-img-top" alt="{{this}}" />
      <div class="card-body">
        <h5 class="card-title">{{this}}</h5>
      </div>
    </div>
  </div>
{{/each}}